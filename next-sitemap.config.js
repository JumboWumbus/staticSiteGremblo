/** @type {import('next-sitemap').IConfig} */

module.exports = {
  siteUrl: process.env.SITE_URL || 'https://gremblo.neocities.org/',
  generateRobotsTxt: true // (optional)
  // Default transformation function
};
